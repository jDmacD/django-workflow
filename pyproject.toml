[project]
name = "django-workflow"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2",
    "unicorn>=2.1.3",
]

[dependency-groups]
dev = [
    "ruff>=0.11.7",
]
